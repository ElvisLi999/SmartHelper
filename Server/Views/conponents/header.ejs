<!-- Main Navigation -->
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <!-- LOGO setting -->
      <a class="navbar-brand mx-auto" href="/">
        <img class="navbar-brand" src="/Assets/images/logo.png" href="/" alt="Smart Helper">
      </a>
      <!-- navbar setting -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-start" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <% if (title == "Tools") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/"><i class="fas fa-xl fa-calculator" style="color: #1E3050"></i><b> Tools </b></a>
          </li>

          <li class="nav-item">
            <% if (title == "AI Tools") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/ailinks"><i class="fas fa-xl fa-robot" style="color: #1E3050"></i><b> AI Tools </b></a>
          </li>

          <li class="nav-item">
            <% if (title == "Blog") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/blog"><i class="fas fa-xl fa-blog" style="color: #1E3050"></i><b> Blog </b></a>
          </li>

          <li class="nav-item">
            <% if (title == "News") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/news"><i class="fas fa-rss fa-xl fa-beat-fade" style="color: #1E3050"></i><b> News </b></a>
          </li>
        </ul>
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Type your keywords here..." aria-label="Search">
            <select name="searchType" class="form-select" style="max-width: 150px;">
              <option value="internal">Internal</option>
              <option value="google">Google</option>
              <option value="bing">Bing</option>
            </select>
          <button class="btn btn-secondary" type="submit" >Search</button>
      </form>
      </div>
    </div>
  </nav>