<!-- Main Navigation -->
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <!-- LOGO setting -->
      <a class="navbar-brand mx-auto" href="/">
        <img class="navbar-brand" src="/Assets/images/logo.png" href="/" alt="Smart Helper">
      </a>
      <!-- navbar setting -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-start" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <% if (title == "Tools") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/"><i class="nav-icon fas fa-xl fa-calculator" ></i><b> Tools </b></a>
          </li>

          <li class="nav-item">
            <% if (title == "AI Tools") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/ailinks"><i class="nav-icon fas fa-xl fa-robot" ></i><b> AI Tools </b></a>
          </li>

          <li class="nav-item">
            <% if (title == "Blog") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/blog"><i class="nav-icon fas fa-xl fa-blog" ></i><b> Blog </b></a>
          </li>

          <li class="nav-item">
            <% if (title == "News") { %>
              <a class="nav-link active"
            <% } else { %>
              <a class="nav-link"
            <% } %>
              href="/news"><i class="nav-icon fas fa-rss fa-xl fa-beat-fade" ></i><b> News </b></a>
          </li>
        </ul>
          <!-- search setting -->
          <form class="d-flex" role="search">
            <input class="form-control ms-5" type="search" placeholder="Type your keywords here..." aria-label="Search">
              <select name="searchType" class="form-select" style="max-width: 150px;">
                <option value="internal">Internal</option>
                <option value="google">Google</option>
                <option value="bing">Bing</option>
              </select>
            <button id="btn btn-light ms-5" type="submit" >Search</button>
        </form>
        <!-- theme setting -->
        <div >
          <button id="themeToggle" class="btn ms-5" >
            <i id="iconLight" class="bi bi-sun-fill" ></i>
            <i id="iconDark" class="bi bi-moon-fill" style="display: none;"></i>
          </button>
        </div>
      </div>
    </div>
  </nav>